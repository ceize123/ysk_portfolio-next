import Input from "./Input";

function SectionCreate({ prop }) {
	console.log(prop);
	return (
		<div>
			{/* <Input prop={type}
				onChange={e => {
					setWork({ ...work, [item]: e.target.value });
				}} /> */}
		</div>
	);
}

export default SectionCreate;